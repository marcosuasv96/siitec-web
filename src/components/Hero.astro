---
// src/components/Hero.astro
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-[#0B1221]">
    
    <canvas id="neural-network" class="absolute inset-0 w-full h-full z-0"></canvas>
    
    <div class="absolute inset-0 bg-gradient-to-r from-[#0B1221] via-[#0B1221]/80 to-transparent z-0"></div>

    <div class="absolute top-24 right-4 md:top-32 md:right-10 w-20 h-20 border-t-2 border-r-2 border-siitec-accent/50 rounded-tr-xl opacity-60"></div>
    <div class="absolute bottom-10 left-4 md:bottom-20 md:left-10 w-20 h-20 border-b-2 border-l-2 border-siitec-light/50 rounded-bl-xl opacity-60"></div>
    <div class="absolute bottom-10 right-4 md:bottom-20 md:right-10 flex flex-col items-end gap-1 opacity-70">
        <div class="flex items-center gap-2">
            <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
            <span class="text-xs font-mono text-siitec-light tracking-widest">SYSTEM: ONLINE</span>
        </div>
        <div class="text-[0.6rem] font-mono text-gray-500">SECURE CONNECTION // Z_NODE_01</div>
    </div>

    <div class="relative z-10 max-w-7xl mx-auto px-4 w-full grid grid-cols-1 lg:grid-cols-2 gap-12 items-center pt-20">
        
       <div class="text-center lg:text-left">
            <div class="inline-flex items-center gap-2 bg-siitec-light/10 border border-siitec-light/20 rounded-full px-4 py-1 mb-6 backdrop-blur-sm">
                <span class="relative flex h-2 w-2">
                  <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-siitec-accent opacity-75"></span>
                  <span class="relative inline-flex rounded-full h-2 w-2 bg-siitec-accent"></span>
                </span>
                <span class="text-siitec-light font-mono text-xs uppercase tracking-wider">Soluciones Integrales</span>
            </div>

            <h1 class="text-5xl lg:text-7xl font-bold leading-tight text-white mb-6 tracking-tight">
                Asesoría y Tecnología <br />
                <span class="text-transparent bg-clip-text bg-gradient-to-r from-siitec-light to-siitec-accent filter drop-shadow-[0_0_10px_rgba(92,164,216,0.5)]">
                    de Alta Calidad
                </span>
            </h1>
            
            <p class="text-lg text-gray-400 mb-8 max-w-lg mx-auto lg:mx-0 leading-relaxed font-light">
                Somos tu aliado estratégico en innovación. Ofrecemos <strong class="text-white font-medium">venta de equipo de cómputo</strong>, implementación de CCTV y <strong class="text-white font-medium">consultoría TI experta</strong> para llevar tu empresa al siguiente nivel.
            </p>
            
            <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
                <a href="/catalogo" class="group relative px-8 py-4 bg-siitec-accent text-siitec-dark font-bold rounded-lg overflow-hidden transition-all hover:scale-105 hover:shadow-[0_0_20px_rgba(249,178,51,0.5)]">
                    <div class="absolute inset-0 w-full h-full bg-white/20 scale-x-0 group-hover:scale-x-100 transition-transform origin-left duration-500"></div>
                    <span class="relative flex items-center gap-2">
                        Ver Catálogo
                        <svg class="w-4 h-4 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path></svg>
                    </span>
                </a>
                
                <a href="/contacto" class="px-8 py-4 border border-white/20 bg-white/5 backdrop-blur-sm text-white rounded-lg hover:bg-white/10 transition flex items-center justify-center gap-2 group">
                    <span class="group-hover:text-siitec-light transition">Solicitar Asesoría</span>
                </a>
            </div>
        </div>

        <div class="hidden lg:flex justify-center relative">
            <div class="absolute w-[500px] h-[500px] border border-siitec-light/10 rounded-full animate-[spin_10s_linear_infinite]"></div>
            <div class="absolute w-[350px] h-[350px] border border-siitec-accent/10 rounded-full animate-[spin_15s_linear_infinite_reverse]"></div>
            
            <div class="relative bg-[#0B1221]/60 backdrop-blur-xl border border-siitec-light/30 p-8 rounded-2xl shadow-2xl max-w-sm transform hover:-translate-y-2 transition duration-500 group">
                <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-transparent via-siitec-light to-transparent opacity-50 animate-[scan_3s_ease-in-out_infinite]"></div>
                
                <div class="flex items-center justify-between mb-6">
                    <div class="flex items-center gap-3">
                        <div class="p-3 bg-siitec-dark rounded-lg text-siitec-accent">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path></svg>
                        </div>
                        <div>
                            <h3 class="text-white font-bold">Siitec Shield</h3>
                            <p class="text-xs text-siitec-light">Vigilancia Activa</p>
                        </div>
                    </div>
                    <span class="text-xs font-mono text-green-400 border border-green-400/30 px-2 py-1 rounded bg-green-400/10">SECURE</span>
                </div>

                <div class="space-y-4">
                    <div class="bg-white/5 p-3 rounded-lg flex items-center justify-between">
                        <span class="text-sm text-gray-300">Cámaras</span>
                        <span class="text-xs text-siitec-accent font-mono">REC ●</span>
                    </div>
                    <div class="bg-white/5 p-3 rounded-lg flex items-center justify-between">
                        <span class="text-sm text-gray-300">Servidores</span>
                        <div class="w-24 h-1.5 bg-gray-700 rounded-full overflow-hidden">
                            <div class="w-[90%] h-full bg-siitec-light"></div>
                        </div>
                    </div>
                    <div class="bg-white/5 p-3 rounded-lg flex items-center justify-between">
                        <span class="text-sm text-gray-300">Sistema</span>
                        <span class="text-xs text-white font-mono">Actualizado</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    const canvas = document.getElementById('neural-network') as HTMLCanvasElement;
    const ctx = canvas.getContext('2d');

    let particlesArray;

    // Ajustar canvas al tamaño de pantalla
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;

    // Manejar redimensionamiento
    window.addEventListener('resize', () => {
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
        init();
    });

    // Ratón
    const mouse = {
        x: null,
        y: null,
        radius: 150
    }

    window.addEventListener('mousemove', (event) => {
        mouse.x = event.x;
        mouse.y = event.y;
    });

    // Clase Partícula
    class Particle {
        x: number;
        y: number;
        directionX: number;
        directionY: number;
        size: number;
        color: string;

        constructor(x, y, directionX, directionY, size, color) {
            this.x = x;
            this.y = y;
            this.directionX = directionX;
            this.directionY = directionY;
            this.size = size;
            this.color = color;
        }

        // Dibujar punto
        draw() {
            ctx.beginPath();
            ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2, false);
            ctx.fillStyle = this.color;
            ctx.fill();
        }

        // Mover punto
        update() {
            // Rebotar en bordes
            if (this.x > canvas.width || this.x < 0) {
                this.directionX = -this.directionX;
            }
            if (this.y > canvas.height || this.y < 0) {
                this.directionY = -this.directionY;
            }

            // Detección de colisión con mouse
            let dx = mouse.x - this.x;
            let dy = mouse.y - this.y;
            let distance = Math.sqrt(dx*dx + dy*dy);

            if (distance < mouse.radius + this.size) {
                if (mouse.x < this.x && this.x < canvas.width - this.size * 10) {
                    this.x += 2;
                }
                if (mouse.x > this.x && this.x > this.size * 10) {
                    this.x -= 2;
                }
                if (mouse.y < this.y && this.y < canvas.height - this.size * 10) {
                    this.y += 2;
                }
                if (mouse.y > this.y && this.y > this.size * 10) {
                    this.y -= 2;
                }
            }

            this.x += this.directionX;
            this.y += this.directionY;
            this.draw();
        }
    }

    // Inicializar partículas
    function init() {
        particlesArray = [];
        // Calcular número de partículas según tamaño de pantalla (menos en móviles)
        let numberOfParticles = (canvas.height * canvas.width) / 9000;
        
        for (let i = 0; i < numberOfParticles; i++) {
            let size = (Math.random() * 2) + 1;
            let x = (Math.random() * ((innerWidth - size * 2) - (size * 2)) + size * 2);
            let y = (Math.random() * ((innerHeight - size * 2) - (size * 2)) + size * 2);
            let directionX = (Math.random() * 1) - 0.5; // Velocidad X
            let directionY = (Math.random() * 1) - 0.5; // Velocidad Y
            
            // Colores aleatorios entre Azul Claro y Amarillo
            let color = Math.random() > 0.5 ? '#5CA4D8' : '#F9B233'; 

            particlesArray.push(new Particle(x, y, directionX, directionY, size, color));
        }
    }

    // Conectar puntos cercanos (La magia de la red neuronal)
    function connect() {
        let opacityValue = 1;
        for (let a = 0; a < particlesArray.length; a++) {
            for (let b = a; b < particlesArray.length; b++) {
                let distance = ((particlesArray[a].x - particlesArray[b].x) * (particlesArray[a].x - particlesArray[b].x))
                + ((particlesArray[a].y - particlesArray[b].y) * (particlesArray[a].y - particlesArray[b].y));
                
                if (distance < (canvas.width/7) * (canvas.height/7)) {
                    opacityValue = 1 - (distance/20000);
                    ctx.strokeStyle = 'rgba(92, 164, 216,' + opacityValue + ')'; // Color de línea (Azul Siitec)
                    ctx.lineWidth = 1;
                    ctx.beginPath();
                    ctx.moveTo(particlesArray[a].x, particlesArray[a].y);
                    ctx.lineTo(particlesArray[b].x, particlesArray[b].y);
                    ctx.stroke();
                }
            }
        }
    }

    // Bucle de animación
    function animate() {
        requestAnimationFrame(animate);
        ctx.clearRect(0,0,innerWidth, innerHeight);

        for (let i = 0; i < particlesArray.length; i++) {
            particlesArray[i].update();
        }
        connect();
    }

    init();
    animate();
</script>

<style>
    /* Animación de escaneo para la tarjeta */
    @keyframes scan {
        0% { top: 0%; opacity: 0; }
        50% { opacity: 0.5; }
        100% { top: 100%; opacity: 0; }
    }
</style>